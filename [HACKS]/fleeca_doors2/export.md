```
    @levels = max 10. how many levels to do
    @tiles = max 8. how many squares
    local result = exports['fleeca_doors2']:startHack(levels, tiles)
    if result then
        -- what do if won
    else
        -- what do if fail
    end
```
